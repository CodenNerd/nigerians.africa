/**
 * Drizzle / Postgres schema mirror of supabase/migrations.
 * The running demo uses @nigeria-for-nigerians/domain seed store.
 * Wire DATABASE_URL here when Supabase local is available.
 */
export const schemaNote =
  "See ../../supabase/migrations/20260319000000_init.sql for the canonical SQL schema.";
